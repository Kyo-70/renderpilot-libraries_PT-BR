// Public Luma v2 helpers. v2 is the first wire contract that carries the
// typed Engine.ini mutation recipes. The authoring model remains shared with
// v1; only the generated projection differs.

export const SCHEMA_VERSION = 2;
